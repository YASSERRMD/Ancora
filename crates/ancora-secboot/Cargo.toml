[package]
name = "ancora-secboot"
version = "0.1.0"
edition = "2021"
description = "Secure boot process integrity verification with attestation, measurement chains, and sealing policies for Ancora"
keywords = ["secure-boot", "integrity", "attestation", "tpm", "security"]
categories = ["cryptography", "data-structures"]
license = "MIT"
repository = "https://github.com/YASSERRMD/Ancora"

[dependencies]
