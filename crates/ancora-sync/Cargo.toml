[package]
name = "ancora-sync"
version = "0.1.0"
edition = "2021"
authors = ["YASSERRMD <arafath.yasser@gmail.com>"]
license = "Apache-2.0"
description = "Offline sync and reconciliation for edge devices in the Ancora agent framework"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
