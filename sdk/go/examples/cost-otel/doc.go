// Package main (cost-otel) shows how to attach lightweight span tracking
// around agent runs to record token counts and durations, mirroring the
// information that an OpenTelemetry exporter would consume.
package main
