// Package main (durable-restart) shows how to persist run state to SQLite
// so that a crashed or restarted process can resume event replay from the
// stored journal without rerunning the agent.
package main
