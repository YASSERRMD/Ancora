# @ancora/sdk-darwin-arm64

Prebuilt native addon for the [ancora](https://www.npmjs.com/package/ancora) TypeScript SDK.

**Platform:** macOS arm64 (Apple Silicon)

This package is automatically installed as an optional dependency of `ancora`.
Do not install it directly.
